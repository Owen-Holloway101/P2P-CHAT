========================================================================================================================

This is documentation for the source files.

Global:

the run method is start when the class is run as a new thread, I will not cover this in each file as it would mean
a lot of repetition to no end. I do for the sake of tidy methods and speedy code.